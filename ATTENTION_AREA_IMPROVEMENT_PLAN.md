# ONF 작업 영역 기반 시각적 주의 판정 개선 계획

## 1. 목적

ONF는 사용자의 인지적 집중이나 휴대폰 사용 여부를 직접 판별하지 않는다. 웹캠에서 얻은 얼굴, 머리 자세, 양쪽 눈의 방향을 이용해 사용자가 설정한 작업 영역을 보고 있는 시간을 추정한다.

초기 지원 작업 영역은 다음 두 가지다.

- 화면 영역: 모니터를 보며 학습하는 영역
- 필기 영역: 종이, 책 또는 태블릿을 보며 학습하는 책상 영역

최종 판정은 다음 원칙을 따른다.

```text
유효한 얼굴 및 양쪽 눈 추적
+ 머리 방향과 눈 방향을 결합한 작업 영역 판정
+ 순간 움직임 및 자세 전환 유예
= 집중 / 주의 이탈 / 측정 불가
```

## 2. 현재 구현 진단

현재 V2는 이전 프로토타입보다 개선됐지만, 화면·필기 모드에서 다음 구조적 한계가 있다.

1. `CalibrationProfile`은 yaw, pitch, gaze 등의 중앙값만 저장한다.
2. 사용자가 캘리브레이션 때 취한 한 자세에 가까운지를 판정한다.
3. 실제 작업 중 발생하는 자연스러운 자세 변화 범위를 저장하지 않는다.
4. 화면·필기 프로필 중 매 프레임 거리가 짧은 프로필을 즉시 선택한다.
5. 프로필이 변경될 때마다 시간 필터가 초기화돼 경계 구간에서 판정이 흔들릴 수 있다.
6. 눈 방향과 머리 방향을 각각 임계값으로 비교할 뿐, 하나의 작업 영역 점수로 결합하지 않는다.
7. 두 기준 어디에도 명확히 속하지 않는 전환 구간을 별도 상태로 처리하지 않는다.

이 때문에 실제로 종이나 화면을 보고 있어도 캘리브레이션 때와 자세가 달라지면 `GAZE_AWAY`, `HEAD_DOWN`, `HEAD_SIDE`가 발생할 수 있다.

## 3. 구현 원칙

### 3.1 자세 기준점이 아니라 작업 영역 분포를 저장한다

화면과 필기를 각각 한 점으로 저장하지 않고, 자연스럽게 작업하는 동안 관측된 값의 중심과 변동 폭을 저장한다.

```python
WorkAreaProfile(
    area="writing",
    center={
        "yaw": ...,
        "pitch": ...,
        "roll": ...,
        "gaze_x": ...,
        "gaze_y": ...,
        "face_x": ...,
        "face_y": ...,
        "face_scale": ...,
    },
    spread={
        "yaw": ...,
        "pitch": ...,
        "roll": ...,
        "gaze_x": ...,
        "gaze_y": ...,
        "face_x": ...,
        "face_y": ...,
        "face_scale": ...,
    },
    sample_count=...,
    quality_score=...,
)
```

중심은 중앙값, 변동 폭은 MAD(Median Absolute Deviation) 또는 분위수 범위를 사용한다. 평균과 표준편차보다 순간적인 랜드마크 튐에 덜 민감하다.

### 3.2 눈 방향을 중심으로 머리 자세를 결합한다

얼굴 위치만으로 집중을 판정하지 않는다. 다음 특징을 결합해 작업 영역 점수를 계산한다.

- 양쪽 눈의 수평·수직 iris 비율
- 왼쪽·오른쪽 눈 시선 일치도
- 머리 yaw, pitch, roll
- 얼굴 중심과 얼굴 크기
- 추적 신뢰도와 눈 열림 상태

가중치는 처음부터 복잡한 학습 모델로 만들지 않는다. 초기 구현은 정규화된 거리의 가중합을 사용한다.

```text
영역 거리 =
  눈 방향 거리      * 높은 가중치
+ 머리 방향 거리    * 중간 가중치
+ 얼굴 위치·거리    * 낮은 가중치
```

얼굴 위치와 크기는 시선 보정 및 품질 판단에 사용하며, 조금 움직였다는 이유만으로 직접 주의 이탈을 만들지 않는다.

### 3.3 화면과 필기를 독립적인 허용 영역으로 취급한다

현재 관측값에 대해 화면 점수와 필기 점수를 모두 계산한다.

```text
화면 영역 안                     -> SCREEN_ATTENTION
필기 영역 안                     -> WRITING_ATTENTION
두 영역 모두 가능                -> 직전 영역 유지
두 영역 사이를 이동              -> AREA_TRANSITION
두 영역 모두에서 지속적으로 이탈 -> ATTENTION_AWAY
판정 신뢰도가 낮음                -> UNSCORED
```

`SCREEN_ATTENTION`과 `WRITING_ATTENTION`은 모두 최종 `FOCUS`로 집계한다. 영역 정보는 UI와 디버그 기록에만 별도로 남긴다.

### 3.4 프로필 전환에는 히스테리시스를 적용한다

매 프레임 가장 가까운 프로필로 즉시 바꾸지 않는다.

- 현재 영역은 다른 영역이 충분히 더 가까울 때만 전환 후보가 된다.
- 전환 후보가 약 0.5~1.0초 지속돼야 실제 영역을 변경한다.
- 화면과 필기 점수 차이가 작으면 직전 영역을 유지한다.
- 영역 변경 시 전체 판정 필터를 무조건 초기화하지 않는다.
- 눈·머리 필터는 유지하고, 영역 전환 후보 타이머만 초기화한다.

정확한 시간과 점수 차이는 설정 UI에 바로 노출하지 않고 내부 기본값으로 시작한다. 실제 사용 데이터로 필요성이 확인된 값만 추후 고급 설정에 추가한다.

### 3.5 자연스러운 자세 조정에는 유예 상태를 둔다

사람이 몸을 펴거나 목을 움직이는 행동을 즉시 비집중으로 계산하지 않는다.

초기 정책안:

| 지속 시간 | 처리 |
|---|---|
| 약 0~2초 | 직전 유효 판정 유지 |
| 약 2~5초 | `AREA_TRANSITION`, 집계는 직전 상태 유지 |
| 약 5초 이상 작업 영역 밖 | `ATTENTION_AWAY` 후보 |
| 이탈 확정 후 정상 영역 복귀 | 약 0.5초 안정 후 `FOCUS` 복귀 |

이 값은 확정값이 아니다. 단위 테스트와 실제 사용으로 조정한다. 장시간 주의 이탈 알림 1분 정책과는 별개다. 짧은 이탈 판정이 누적된 뒤 1분이 됐을 때 알림을 울린다.

### 3.6 자동 적응은 제한적으로만 허용한다

현재 자세를 무조건 기준으로 학습하면 옆 보기나 휴대폰 보기까지 정상으로 흡수할 수 있다. 따라서 초기 구현에서는 자동 적응을 비활성화하고, 영역 분포와 전환 안정화부터 검증한다.

추후 자동 적응은 다음 조건을 모두 만족할 때만 적용한다.

- 얼굴과 양쪽 눈의 추적 신뢰도가 높음
- 눈이 열린 상태
- 기존 작업 영역에서 크게 벗어나지 않음
- 최근 수 초 동안 동일 작업 영역으로 안정적으로 판정됨
- 변화가 갑작스럽지 않고 서서히 발생함
- 초기 캘리브레이션 범위를 기준으로 최대 이동량 제한

자동 적응으로 해결할 수 없는 큰 변화는 기준 재설정을 안내한다.

## 4. 캘리브레이션 UX 변경

### 4.1 화면 영역 설정

목표는 한 자세를 고정하는 것이 아니라 사용자가 화면을 볼 때 발생하는 자연스러운 범위를 수집하는 것이다.

1. 평소 화면을 보는 자세를 취한다.
2. 화면 중앙과 실제로 자주 보는 좌우·위아래 영역을 자연스럽게 본다.
3. 약 5~7초 동안 유효 샘플을 수집한다.
4. 양쪽 눈 검출률, 자세 범위, 얼굴 거리와 샘플 수를 검증한다.
5. 너무 움직였다는 이유만으로 실패시키지 않고, 작업 영역으로 보기 어려울 정도로 분산이 큰 경우에만 실패한다.

### 4.2 필기 영역 설정

1. 종이, 책 또는 태블릿을 실제 사용할 위치에 둔다.
2. 한 점을 보며 멈추지 않고 실제 필기·읽기처럼 시선을 자연스럽게 움직인다.
3. 약 7~10초 동안 머리와 눈의 범위를 수집한다.
4. 화면 영역과 충분히 구분되는지 확인한다.
5. 범위가 지나치게 넓거나 눈 검출이 불안정하면 실패 원인과 해결 방법을 표시한다.

### 4.3 사용자 피드백

캘리브레이션 중 상태창과 버튼 게이지에 다음 정보를 표시한다.

- `화면 영역 수집 중 64%`
- `필기 영역 수집 중 82%`
- `양쪽 눈을 확인할 수 없습니다. 조명이나 카메라 각도를 조정하세요.`
- `얼굴이 프레임 밖으로 잘렸습니다.`
- `필기 영역을 자연스럽게 보면서 잠시 움직여 주세요.`
- `화면 영역 설정 완료` / `필기 영역 설정 완료`

성공과 실패 시 기존 알림음 시스템을 이용해 서로 다른 피드백을 준다.

## 5. 판정 파이프라인

```text
CameraService
  -> FaceAnalyzer
  -> QualityGate
  -> AttentionFeatureExtractor
  -> TemporalFilter
  -> WorkAreaMatcher
  -> AreaStateStabilizer
  -> FocusPolicy
  -> SessionManager / Recorder / UI
```

### 단계별 책임

1. `FaceAnalyzer`: 얼굴 랜드마크, 머리 자세, 양쪽 눈 방향과 눈 열림을 계산한다.
2. `QualityGate`: 카메라 오류, 얼굴 없음, 다중 얼굴, 눈 추적 불안정을 분리한다.
3. `AttentionFeatureExtractor`: 머리·눈·얼굴 특징을 동일한 데이터 구조로 만든다.
4. `TemporalFilter`: 머리에는 EMA, 눈에는 짧은 중앙값 필터를 적용한다.
5. `WorkAreaMatcher`: 화면과 필기 영역별 거리 및 신뢰도를 계산한다.
6. `AreaStateStabilizer`: 전환 유예와 영역 선택 히스테리시스를 처리한다.
7. `FocusPolicy`: 작업 영역이면 `FOCUS`, 지속 이탈이면 `NON_FOCUS`, 측정 실패면 `UNSCORED`로 변환한다.

눈을 감은 동안에는 iris 기반 영역 판정을 사용하지 않는다. 짧은 깜빡임은 직전 판정을 유지하고, 장시간 눈 감김은 기존 눈 감김 정책으로 처리한다.

## 6. 데이터 모델 변경안

### `onf_v2/core/models.py`

추가 또는 변경 대상:

- `WorkArea`: `SCREEN`, `WRITING`, `TRANSITION`, `OUTSIDE`, `UNKNOWN`
- `AttentionFeatures`: 필터 전·후 공통 특징
- `FeatureDistribution`: 중앙값, MAD/분위수, 유효 범위
- `WorkAreaProfile`: 영역별 특징 분포와 품질
- `AreaMatch`: 영역, 거리, 신뢰도, 각 특징의 기여도
- `FocusDecision`: 현재 작업 영역과 영역 신뢰도 추가

기존 저장 데이터와의 호환을 위해 `CalibrationProfile`을 즉시 삭제하지 않는다. 새 프로필 로딩에 실패하거나 구형 프로필이면 UI에서 재설정을 요구한다.

## 7. 파일별 변경 범위

### 핵심 수정

- `onf_v2/core/models.py`: 작업 영역 및 분포 데이터 모델 추가
- `onf_v2/core/calibration.py`: 단일 중앙점 생성에서 영역 분포 생성으로 변경
- `onf_v2/core/attention_engine.py`: 매 프레임 최근접 프로필 선택 제거, 영역 매처와 전환 안정화 연결
- `onf_v2/core/focus_policy.py`: 작업 영역 상태를 최종 집중 상태로 변환
- `onf_v2/core/temporal_filter.py`: 프로필 전환과 독립적으로 필터가 유지되도록 수정
- `onf_v2/core/face_analyzer.py`: 양쪽 눈 개별 시선값과 일치도, 유효성 정보를 명시적으로 제공

### 새 모듈 후보

- `onf_v2/core/work_area_matcher.py`: 영역별 거리·신뢰도 계산
- `onf_v2/core/area_stabilizer.py`: 화면/필기 전환과 이탈 유예 처리

역할이 충분히 작으면 두 클래스를 `attention_engine.py` 안에 먼저 구현한다. 코드가 복잡해질 때만 별도 파일로 분리한다.

### UI 수정

- `onf_v2/ui/main_window.py`: 현재 작업 영역과 전환·오류 상태를 사용자 문구로 표시
- 일반 화면에는 각도나 iris 수치를 노출하지 않는다.
- 디버그 정보가 필요하면 기존 상태창이 아니라 별도 접이식 진단 영역이나 로그 파일을 사용한다.

### 기록 수정

- `onf_v2/app/session_recorder.py`: `matched_area`, `area_confidence`, `screen_score`, `writing_score` 기록
- 기존 영상 저장은 요구하지 않는다.
- 숫자 관측 로그만으로 동일 판정 로직을 재실행할 수 있게 한다.

## 8. 단계별 구현 계획과 성공 기준

### 0단계: 현재 오판 원인 관측

작업:

- 최근 세션의 `observations.csv`에서 필기 중 주된 이탈 원인이 gaze, pitch, yaw 중 무엇인지 확인한다.
- 화면·필기 프로필까지의 거리와 선택된 프로필을 숫자 로그에 추가한다.
- 개인정보 보호를 위해 영상은 저장하지 않는다.

성공 기준:

- 필기 중 `주의 이탈`이 어떤 특징 때문에 발생했는지 한 행의 로그로 설명할 수 있다.
- 화면·필기 프로필이 경계에서 반복 전환되는지 확인할 수 있다.

### 1단계: 프로필 전환 흔들림 수정

작업:

- 현재 영역 유지 우선 규칙 추가
- 전환 점수 여유와 최소 유지시간 추가
- 영역 전환 때 전체 `TemporalFilter`를 초기화하는 로직 제거
- `AREA_TRANSITION` 내부 상태 추가

성공 기준:

- 경계값이 흔들려도 화면/필기 영역이 프레임마다 교대하지 않는다.
- 화면에서 필기로 이동하면 설정된 유예 뒤 한 번만 전환된다.
- 전환 과정의 짧은 구간이 주의 이탈 이벤트로 기록되지 않는다.

### 2단계: 점 기준을 분포 기반 작업 영역으로 교체

작업:

- 캘리브레이션 샘플에서 중앙값과 MAD/분위수 계산
- 화면과 필기의 `WorkAreaProfile` 생성
- 영역별 정규화 거리와 신뢰도 계산
- 눈 방향에 가장 높은 가중치, 머리 자세에 중간 가중치 적용
- 얼굴 위치·크기는 낮은 가중치와 품질 검사에 사용

성공 기준:

- 캘리브레이션 때와 얼굴 위치가 조금 달라도 동일 작업 영역으로 판정된다.
- 필기 중 자연스러운 pitch와 gaze 변화가 프로필 분포 안에서는 집중으로 유지된다.
- 명확하게 옆이나 위를 지속적으로 보면 작업 영역 밖으로 판정된다.

### 3단계: 캘리브레이션 UX 개선

작업:

- 화면은 5~7초, 필기는 7~10초 동안 자연스러운 영역 수집
- 유효 샘플 수와 양쪽 눈 검출률 표시
- 실패 원인별 해결 문구 제공
- 기존 독립 버튼 게이지와 소리 피드백 연결

성공 기준:

- 사용자가 고정 자세를 유지해야 한다고 오해하지 않는다.
- 다른 탭에서도 버튼 게이지와 소리로 진행·완료 여부를 알 수 있다.
- 설정 실패 시 사용자가 다음에 무엇을 해야 하는지 알 수 있다.

### 4단계: 눈·머리 결합 품질 강화

작업:

- 양쪽 눈 시선값 차이로 시선 신뢰도 계산
- 한쪽 눈이 가려지거나 iris 값이 눈 영역 밖으로 튀면 `UNSCORED`
- 머리 자세 변화로 인한 눈 비율 변화를 영역 점수에서 보정
- 눈 감김 중 시선 판정 차단

성공 기준:

- 안경 반사나 한쪽 눈 추적 실패가 즉시 사용자 비집중으로 누적되지 않는다.
- 고개와 눈이 함께 작업 영역을 향할 때 한 신호의 작은 오차로 이탈하지 않는다.
- 추정 불가능한 프레임은 `UNSCORED`로 분리된다.

### 5단계: 제한적 자동 적응 검토

이 단계는 1~4단계를 실제로 사용한 뒤 필요성이 확인된 경우에만 구현한다.

작업:

- 높은 신뢰도의 동일 영역 상태에서만 중심과 범위를 매우 느리게 보정
- 초기 캘리브레이션 대비 최대 이동 한도 적용
- 급격한 자세 변화, 이탈 상태, 낮은 신뢰도에서는 학습 금지
- 적응 전·후 값을 세션 로그에 기록

성공 기준:

- 장시간 사용 중 서서히 달라진 자세를 허용한다.
- 작업 영역 밖 자세를 오래 유지해도 정상 영역으로 흡수하지 않는다.
- 적응을 껐을 때와 비교 가능한 로그가 남는다.

## 9. 테스트 계획

### 단위 테스트

- 화면 영역 중심과 범위 내부 판정
- 필기 영역 중심과 범위 내부 판정
- 화면·필기 경계에서 직전 영역 유지
- 전환 최소 유지시간 전후 판정
- 작업 영역 밖 지속시간 전후 판정
- 양쪽 눈 불일치 및 눈 감김 처리
- 얼굴 위치·크기의 작은 변화 허용
- 자동 적응 한도와 금지 조건(5단계 구현 시)

### 숫자 관측 재생 테스트

녹화 영상은 필수가 아니다. `observations.csv`의 숫자 관측값을 재생해 같은 입력이 항상 같은 결과를 만드는지 확인한다.

필요 시나리오:

- 화면을 자연스럽게 보는 구간
- 화면에서 필기로 이동하는 구간
- 여러 자세로 필기하는 구간
- 잠시 몸을 펴거나 목을 움직이는 구간
- 작업 영역 밖을 지속적으로 보는 구간
- 눈 감김, 얼굴 없음, 낮은 신뢰도 구간

### 수동 검증

- Camo 또는 일반 웹캠에서 동일하게 동작
- 카메라가 화면 중앙이 아니어도 실제 사용자 위치에서 캘리브레이션 가능
- 화면과 필기를 각각 10분 이상 사용했을 때 불필요한 프로필 전환이 반복되지 않음
- 주의 이탈 표시가 나오면 상태창에서 사용자 수준의 원인을 확인 가능
- 앱 프레임 속도와 UI 반응성이 기존보다 악화되지 않음

전체 자동 테스트:

```bash
python -m unittest discover -s tests
```

## 10. 성능 기준

- 얼굴 분석은 UI 스레드를 막지 않는다.
- 영역 점수 계산은 단순 수치 연산으로 유지한다.
- 매 프레임 DB에 직접 쓰지 않고 기존 기록 버퍼 또는 CSV 흐름을 사용한다.
- 영역 분포의 샘플 전체를 매 프레임 비교하지 않고 미리 계산한 중심과 범위를 사용한다.
- 현재 분석 FPS와 UI FPS를 별도로 측정해 병목을 확인한다.

## 11. 하지 않을 것

초기 개선 범위에서는 다음을 구현하지 않는다.

- 휴대폰 사용 여부 판별
- 영상 녹화 또는 영상 업로드
- 실제 인지적 집중도라고 주장하는 기능
- 딥러닝 기반 화면 좌표 시선 추정 모델 추가
- 사용자가 보정할 수 없는 복잡한 자동 학습
- 모든 임계값을 설정 화면에 노출

실제 화면 좌표상의 응시점을 추정하는 다점 시선 캘리브레이션은 작업 영역 분류만으로 부족하다는 것이 확인될 때 별도 단계로 검토한다. 현재 목적에는 화면/필기 영역 분류가 구현 부담과 정확도 사이에서 더 현실적이다.

## 12. 완료 정의

다음 조건을 모두 만족하면 작업 영역 기반 판정 1차 개선을 완료한 것으로 본다.

1. 화면과 필기 캘리브레이션이 각각 한 점이 아닌 실제 변동 범위를 저장한다.
2. 눈 방향이 영역 판정의 중심 신호이고 머리 자세와 얼굴 정보가 이를 보조한다.
3. 화면과 필기 사이를 움직일 때 즉시 주의 이탈이 발생하지 않는다.
4. 필기 중 자연스러운 자세 변화가 허용 범위 안에서 집중으로 유지된다.
5. 두 작업 영역 밖을 일정 시간 이상 볼 때만 주의 이탈로 확정된다.
6. 눈 추적 실패와 사용자 주의 이탈이 구분된다.
7. 프로필 선택이 프레임마다 흔들리지 않는다.
8. 집중도 시간 누적과 1분 장기 이탈 알림 정책이 기존 세션·뽀모도로 기능과 충돌하지 않는다.
9. 숫자 관측 재생 테스트와 전체 단위 테스트가 통과한다.
10. UI에서 현재 화면/필기/전환/측정 불가 상태를 직관적으로 확인할 수 있다.

## 13. 권장 실행 순서

```text
0. 현재 오판 원인 로그 확인
1. 프로필 전환 히스테리시스와 전환 상태
2. 분포 기반 화면·필기 작업 영역
3. 캘리브레이션 UX
4. 양쪽 눈 및 영역 신뢰도 강화
5. 실제 사용 평가
6. 필요할 때만 제한적 자동 적응
```

가장 먼저 수정할 것은 임계값을 단순히 넓히는 작업이 아니다. 현재 오판 원인을 기록할 수 있게 만든 뒤, 프로필 전환 흔들림과 점 기준 모델을 차례로 교체해야 한다. 그래야 필기 판정이 느슨해지는 대신 실제 이탈까지 놓치는 문제를 방지할 수 있다.

## 14. UI 기술 선택

### 14.1 결론

ONF의 판정 엔진, 세션 관리, SQLite 기록은 Python으로 유지하고 UI만 `Qt Quick/QML`로 전환하는 것을 권장한다.

```text
Python
  - 카메라 및 얼굴 분석
  - 작업 영역 판정
  - 세션·뽀모도로
  - 플래너 및 SQLite
  - 설정과 알림

Qt Quick/QML
  - 전체 화면 구조
  - 버튼과 입력 컨트롤
  - 상태 전환과 애니메이션
  - 그래프와 타임라인
  - 반응형 레이아웃
```

QML은 HTML이 아니지만 선언형 UI, 상태, 전환, 속성 애니메이션을 지원한다. Qt Quick은 장면 그래프 기반으로 렌더링하므로 기존 QWidget의 스타일시트와 수동 갱신 방식보다 부드러운 전환과 사용자 정의 화면을 만들기에 적합하다.

Python을 C나 C++로 다시 작성할 필요는 없다. OpenCV와 MediaPipe의 무거운 계산은 이미 네이티브 구현을 사용한다. 실제 성능 측정에서 Python 코드가 병목으로 확인된 경우에만 해당 계산 부분을 제한적으로 네이티브 코드로 옮긴다.

### 14.2 대안 비교

| 선택지 | 장점 | 단점 | 판단 |
|---|---|---|---|
| 기존 PySide6 QWidget 유지 | 변경량이 가장 적음 | 고급 전환, 반응형 구성, 그래프 표현을 계속 수작업해야 함 | 유지보수 단계에만 사용 |
| PySide6 + Qt Quick/QML | Python 엔진 재사용, 부드러운 애니메이션, GPU 가속, 단일 런타임 | QML 화면을 새로 작성해야 함 | 권장 |
| PySide6 + QWebEngineView + HTML/React | 실제 HTML/CSS 생태계 사용 가능 | Chromium 메모리, Python-JS 브리지, 카메라 복사, 패키징 복잡도 증가 | 현재는 비추천 |
| Tauri/웹 UI + Python 보조 프로세스 | 웹 UI 자유도와 비교적 작은 셸 | Rust·웹·Python 다중 구조와 프로세스 관리 필요 | 제품 배포 요구가 커질 때 재검토 |
| Electron + Python | 웹 개발 자료가 많음 | 메모리와 배포 크기가 크고 런타임이 중복됨 | 비추천 |
| C++ Qt/QML 전면 재작성 | 최대 성능 제어 | 개발 비용과 결함 위험이 지나치게 큼 | 필요 없음 |

실제 HTML 프런트엔드도 기술적으로 가능하다. `QWebEngineView` 안에 HTML을 띄우고 `QWebChannel`로 Python 객체와 JavaScript를 연결할 수 있다. 그러나 ONF는 실시간 카메라, 네이티브 단축키, 알림음, 설정, SQLite, 창 상태가 긴밀히 연결돼 있어 전체 UI를 웹으로 옮기지 않는 한 혼합 구조의 복잡성이 더 커진다.

## 15. QML 전환 구조

### 15.1 Python과 UI 경계

QML에서 판정 엔진 객체에 직접 접근하지 않는다. UI 전용 `QObject` 뷰 모델을 둔다.

```text
AttentionEngine / SessionManager / PlannerRepository
                         |
                         v
                    AppViewModel
        properties / signals / invokable commands
                         |
                         v
                       QML UI
```

`AppViewModel`이 제공할 값:

- 현재 앱 상태와 작업 영역
- 집중, 주의 이탈, 휴식, 측정 불가 시간
- 캘리브레이션 상태와 진행률
- 뽀모도로 단계, 남은 시간, 완료 회차
- 오늘 학습 요약과 그래프 데이터
- 플래너 항목과 타임라인
- 설정값과 카메라 연결 상태

QML에서 Python으로 전달할 명령:

- 화면/필기 영역 설정
- 세션 시작·휴식·재개·종료
- 카메라 표시 켜기·끄기
- 뽀모도로 시작·정지
- 플래너 항목 추가·수정·삭제·상태 변경
- 설정 저장과 알림음 테스트

### 15.2 카메라 프레임

현재 OpenCV 카메라 서비스는 유지한다. 표시용 프레임은 `QQuickImageProvider` 또는 동등한 QML 이미지 공급 계층으로 전달한다.

- 카메라 캡처와 얼굴 분석은 UI 스레드 밖에서 실행
- 화면 표시 프레임은 최대 약 24~30 FPS로 제한
- 얼굴 분석은 필요한 수준인 약 10~15 FPS로 독립 실행
- QML은 최신 프레임만 표시하고 오래된 프레임을 순서대로 밀어내지 않음
- 카메라를 숨겨도 분석은 계속할 수 있고, 표시용 변환·복사만 중단
- 이미지 공급자는 여러 스레드에서 호출될 가능성을 고려해 안전하게 구현

카메라 렉 문제를 디자인 전환으로 가리지 않는다. 프레임 캡처, 얼굴 분석, 이미지 변환, UI 표시 시간을 각각 측정한 뒤 병목을 분리한다.

### 15.3 그래프

그래프는 범용 차트 라이브러리에 화면을 맞추지 않고 사용 목적에 맞는 QML 컴포넌트로 구현한다.

- 측정 탭: 왼쪽부터 채워지는 고정 폭 세로 막대
- 집중률 막대: 낮으면 빨강, 중간은 노랑, 높으면 초록으로 연속 보간
- 휴식: 회색 또는 빈 슬롯
- 측정 불가: 옅은 중립색과 별도 범례
- 학습 화면 타임라인: 현재 시각 기준 이전 3시간·현재 1시간·다음 3시간을 10분 칸으로 표시하고, 시간이 넘어가면 창도 자동 이동
- 학습 화면 타임라인의 점선 칸은 계획, 채운 칸은 실제 학습 집중도, 파란 실선은 현재 10분 구간으로 표현
- 일간: 24시간 타임라인, 원형 요약, 정확한 시간 수치
- 주간 학습시간: 7개의 균등한 세로 막대, 날짜와 요일, 이전·다음 주 이동
- 주간 상태 비교: 집중률, 순공 비율, 이탈 비율, 측정 커버리지를 같은 0~100% 축의 꺾은선으로 비교
- 주간 그래프에서 선택한 날짜의 총 학습·순공·이탈 실제 시간은 툴팁과 하단 요약에 함께 표시
- 시간 단위와 비율 단위를 한 축에 섞지 않고, 총 학습시간 막대와 상태 비율 꺾은선 영역을 분리
- 월간: 월 단위 학습 잔디와 요약
- 플래너: 계획 테두리와 실제 학습 집중도 막대를 분리해 중첩 표시

막대 수가 적으면 `Repeater + Rectangle`, 데이터가 많으면 `Canvas` 또는 사용자 정의 장면 그래프 컴포넌트를 사용한다. 매 프레임 전체 그래프를 다시 생성하지 않는다.

## 16. 시각 디자인 원칙

### 16.1 방향

- 밝은 중립 배경과 흰색 작업 면
- 본문은 검정에 가까운 중립색, 보조 문구는 중간 회색
- 주요 동작은 선명한 초록 또는 파랑 한 계열
- 경고색은 실제 주의·오류 상태에만 사용
- 카드 남용 없이 화면 구획과 여백으로 정보 구조 표현
- 카드 모서리는 최대 8px를 기본으로 사용
- 그림자는 약하게 사용하고 테두리와 배경 차이로 계층 표현
- 한국어 UI에 적합한 일관된 글꼴 크기와 행간
- 내부 enum, DB 용어, 디버그 수치를 일반 사용자 화면에서 제거

토스나 네이버의 색상을 복제하지 않는다. 정보 우선순위, 충분한 여백, 명확한 버튼 상태, 짧은 피드백, 일관된 컴포넌트라는 원칙만 참고한다.

### 16.2 디자인 토큰

QML 전역 토큰 파일에서 다음 값을 한 곳에서 관리한다.

```text
색상: background, surface, text, muted, border, primary, success, warning, danger
간격: 4, 8, 12, 16, 24, 32
반경: 4, 6, 8
글꼴: title, section, body, caption, metric
애니메이션: instant, fast, normal, slow
그림자: low, raised
```

화면 파일에 임의 색상과 여백 숫자를 반복해서 넣지 않는다.

### 16.3 화면 밀도와 반응형 규칙

- 측정 화면은 기본 창 높이에서 세로 스크롤 없이 핵심 기능을 모두 표시
- 세션 조작 버튼은 항상 고정된 상단 영역에 유지
- 기록과 설정 탭은 정보량 때문에 필요한 경우에만 내부 스크롤 허용
- 창이 작아지면 글자를 무작정 축소하지 않고 열 배치와 보조 정보를 재구성
- 최소 지원 크기를 정하고 그 이하에서는 창 크기를 제한
- 1024x680, 1280x720, 1366x768, Windows 125%·150% 배율 검증
- 긴 한국어 문구, 큰 시간 값, 오류 메시지에서 겹침과 잘림 검증

## 17. 애니메이션과 입력 피드백

애니메이션은 장식이 아니라 상태 변화와 조작 결과를 설명하는 데 사용한다.

### 버튼

- hover: 배경 또는 테두리 변화, 약 100~140ms
- press: 크기 약 0.98 또는 음영 변화, 약 70~100ms
- release: 원래 상태로 자연스럽게 복귀
- 실행 중: 진행 게이지 또는 로딩 상태
- 성공: 짧은 색상 전환과 체크 아이콘
- 실패: 흔들기보다 명확한 오류 문구와 경고색 사용
- 비활성: 색상뿐 아니라 커서와 툴팁으로 이유 제공

버튼 클릭 직후 명령이 실행됐다는 시각 피드백을 먼저 주고, 실제 완료 결과는 별도 상태로 표시한다. 카메라 연결이나 캘리브레이션처럼 시간이 걸리는 동작은 눌린 상태에서 아무 변화 없이 기다리게 하지 않는다.

### 화면 전환

- 탭 전환: 약 160~220ms의 짧은 투명도 및 8~12px 이동
- 상태 패널 변경: 색상과 문구를 약 150~200ms로 전환
- 팝업과 메뉴: 투명도와 작은 크기 변화
- 그래프 갱신: 막대 높이를 약 200~300ms로 보간
- 타이머 숫자: 매초 과도하게 움직이지 않고 값만 안정적으로 변경

무한 반복 장식, 큰 탄성 효과, 긴 페이지 전환은 사용하지 않는다. 실시간 카메라와 판정이 실행 중일 때 애니메이션이 분석 성능을 떨어뜨리지 않도록 opacity와 transform 중심으로 구성한다.

### 접근성과 피드백

- 색상만으로 집중·휴식·오류를 구분하지 않음
- 키보드 포커스 표시
- 클릭 영역 최소 크기 유지
- 알림음과 화면 피드백을 함께 제공
- 시스템 애니메이션 축소 설정을 반영할 수 있는 전역 옵션 마련

## 18. UI 전환 단계

### UI 0단계: 기능 및 화면 목록 고정

- 측정, 기록, 플래너, 설정 화면의 필수 기능 목록 확정
- 중복된 카드, 문구, 버튼 제거
- 각 화면의 빈 상태, 오류, 로딩, 비활성 상태 정의
- 현재 사용자 흐름과 충돌 여부 확인

화면 역할은 다음처럼 한 번만 소유한다.

- 학습: 현재 시각, 지금 할 일, 오늘 할 일, 자유 학습·뽀모도로 타이머, 학습 조작, 카메라 표시, 최소 집중 상태
- 플래너: 계획 추가·시간 편집·상태 변경과 계획 타임라인
- 기록: 완료된 학습의 일간·주간·월간 결과와 비교 그래프
- 설정: 카메라 장치, 작업 방식, 판정 민감도, 알림, 뽀모도로 기본값
- 캘리브레이션은 학습 화면에 상시 관리 도구로 두지 않고, 필요한 경우에만 맥락 안내와 설정 동작을 표시
- 상세 측정 수치, DB 용어, 캘리브레이션 품질, 기간 통계는 학습 화면에서 제거

성공 기준:

- 모든 기능이 어느 화면에 속하는지 한 번만 정의된다.
- 홈 화면은 별도로 만들지 않는다. 반복 사용의 중심인 학습 화면을 기본 시작 화면으로 사용한다.

### UI 1단계: 디자인 토큰과 QML 프로토타입

- Python 엔진과 연결하지 않은 측정 화면 QML 프로토타입 작성
- 상단 세션 제어, 카메라, 상태, 뽀모도로, 그래프 배치 검증
- 버튼 press/hover/loading/success/error 상태 구현
- 목표 해상도별 스크린샷 검증

성공 기준:

- 1024x680 이상에서 측정 페이지에 불필요한 스크롤이 없다.
- 모든 버튼이 눌림과 처리 상태를 즉시 표현한다.
- 카메라·상태·뽀모도로 우선순위가 모드에 따라 명확하다.

### UI 2단계: Python-QML 브리지

- `AppViewModel`과 화면별 하위 모델 구현
- 기존 엔진의 상태를 읽기 전용 속성과 신호로 노출
- QML 명령을 기존 세션 메서드에 연결
- UI에서 비즈니스 규칙을 계산하지 않도록 제한

성공 기준:

- 같은 명령을 기존 UI와 QML UI에서 실행했을 때 세션 결과가 동일하다.
- QML을 다시 로드해도 엔진·DB 데이터가 손상되지 않는다.

### UI 3단계: 측정 화면 전환

- 카메라 이미지 공급 연결
- 화면/필기 캘리브레이션 진행 표시
- 세션·휴식·종료·뽀모도로 흐름 연결
- 실시간 세로 막대 그래프 연결
- 오류와 측정 불가 상태 표시

성공 기준:

- 30분 사용 중 UI 멈춤이나 프레임 큐 누적이 없다.
- 카메라 숨김 상태에서 분석은 유지되고 UI 부하는 감소한다.
- 세션 종료 시 뽀모도로와 모든 진행 상태가 함께 정리된다.

### UI 4단계: 기록·플래너·설정 전환

- 기록 일간/주간/월간 대시보드
- 플래너 계획 목록과 실제 학습 타임라인
- 날짜·시간 입력과 선택적 계획 시간
- 설정 저장, 카메라 선택, 볼륨 및 알림음 테스트

성공 기준:

- 기존 SQLite 기록과 설정을 그대로 읽는다.
- 기록 기간 이동과 플래너 편집이 즉시 반영된다.
- 설정·기록 탭의 스크롤은 해당 내용 영역 안에서만 동작한다.

### UI 5단계: 기존 QWidget UI 제거

모든 주요 흐름이 QML에서 검증된 뒤 기존 `main_window.py` 기반 화면을 제거한다. 전환 중에는 기존 UI를 비교용으로 유지하되 두 UI를 동시에 확장하지 않는다.

성공 기준:

- 기능 동등성 체크리스트와 자동 테스트 통과
- Windows 실행 파일에서 QML 리소스와 글꼴 누락 없음
- 앱 재시작, 카메라 실패, 부분 세션 저장, DB 오류 흐름 검증

## 19. 디자인 완료 정의

1. 측정 화면의 핵심 조작이 스크롤과 관계없이 항상 보인다.
2. 버튼은 hover, press, 처리 중, 성공, 실패, 비활성 상태를 가진다.
3. 상태 변화가 0.2초 안에 화면에 반영된다.
4. 애니메이션은 UI 스레드와 얼굴 분석을 방해하지 않는다.
5. 카메라 화면, 뽀모도로, 집중 그래프가 현재 모드에 맞는 우선순위를 가진다.
6. 기록 화면은 디버그 DB 뷰가 아니라 오늘·주간·월간 학습 요약을 보여준다.
7. 플래너는 계획과 실제 학습 타임라인을 분리해서 보여준다.
8. 최소 지원 크기와 Windows DPI 배율에서 요소가 잘리거나 겹치지 않는다.
9. 일반 사용자 화면에 내부 상태명과 원시 수치가 노출되지 않는다.
10. 기존 세션, 뽀모도로, 플래너, 알림, 설정 기능이 디자인 전환 전과 동일하게 동작한다.

## 20. UI 기술 참고 자료

- [Qt Quick 상태·전환·애니메이션](https://doc.qt.io/qtforpython-6/overviews/qtquick-statesanimations-topic.html)
- [Qt Quick 장면 그래프와 QQuickWindow](https://doc.qt.io/qtforpython-6/PySide6/QtQuick/QQuickWindow.html)
- [QML용 비동기 이미지 공급자](https://doc.qt.io/qtforpython-6/PySide6/QtQuick/QQuickImageProvider.html)
- [PySide6 QML 애플리케이션 구성](https://doc.qt.io/qtforpython-6/tutorials/qmlapp/qmlapplication.html)
- [HTML 내장 대안 QWebEngineView](https://doc.qt.io/qtforpython-6/PySide6/QtWebEngineWidgets/QWebEngineView.html)
- [HTML과 Qt 객체 통신 QWebChannel](https://doc.qt.io/qtforpython-6/overviews/qtwebchannel-javascript.html)
